<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.medical3d.viewer.modules.asset.mapper.ModelAssetMapper">
  <select id="findReadyByResultId" resultType="com.medical3d.viewer.modules.asset.entity.ModelAssetEntity">
    SELECT id, result_id, layer_id, variant, provider, bucket, object_key, format, compression,
           size_bytes, triangle_count, checksum, version, status, created_at, updated_at
    FROM model_asset WHERE result_id = #{resultId} AND status = 'READY'
    ORDER BY layer_id, version DESC
  </select>
</mapper>
